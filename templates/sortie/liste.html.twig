{% extends 'base.html.twig' %}

{% block title %}Liste sortie{% endblock %}
{% block body %}

    <section>
        <table>
            <thead>
            <tr>
                <th>Nom:</th>
                <th>date heure début :</th>
                <th>durée:</th>
                <th>date limite d'inscription:</th>
                <th>Nombre d'inscription max:</th>
                <th>Info sortie:</th>
                <th>Etat:</th>
            </tr>
            </thead>
        </table>
    </section>
    <p>ici la liste des sortie</p>
    {% for sortie in sorties %}
    <p>{{ sortie.nom }}</p>
    <p>{{ sortie.date_heure_debut }}</p>
    <p>{{ sortie.duree }}</p>
    <p>{{ sortie.date_limiteinscription }}</p>
    <p>{{ sortie.nb_inscription_max }}</p>
    <p>{{ sortie.info_sortie }}</p>
    <p>{{ sortie.etat }}</p>
    {% endfor %}
{% endblock body %}